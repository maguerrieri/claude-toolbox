---
description: 'Use when asked to adopt, inspect, or clear a ticket-workflow role charter in the current task'
argument-hint: '<planner | epic-coordinator | implementer | none>'
---
Set or clear the current task's role charter: **$ARGUMENTS**

Invoke the `ticket-workflow` skill now via the Skill tool and run its
authoritative **ROLE mini-phase (`/role`)** with `$ARGUMENTS`. The command is
only a delegate: do not restate or implement role validation, resource lookup,
persistence, clearing, or reporting here.
