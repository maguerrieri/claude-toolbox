{
  "description": "Session roles — export the session id as a fallback for CLIs that don't set CLAUDE_CODE_SESSION_ID, re-inject the charter across resume and compaction, guard a pinned planner against drifting into implementation, stop a pinned implementer from launching issue-spawning sessions, and keep in-process subagents from writing their parent's role marker.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact|fork",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/role-session-start.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "^(Edit|Write|MultiEdit|NotebookEdit|Bash|create_session|mcp__.*__create_session)$",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/role-guard.sh\""
          }
        ]
      }
    ]
  }
}
